/**
 * Computes layout positions for workflow graph nodes using dagre.
 *
 * Stub — created for CT-02 red phase test compilation.
 * Implementation will be provided in the green phase.
 */

import type { WorkflowNode, WorkflowEdge } from './workflow-types.js';

export function layoutGraph(_nodes: WorkflowNode[], _edges: WorkflowEdge[]): WorkflowNode[] {
	throw new Error('layoutGraph is not implemented');
}
