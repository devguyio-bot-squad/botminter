/**
 * Parses a ralph.yml string into a WorkflowGraph.
 *
 * Stub — created for CT-02 red phase test compilation.
 * Implementation will be provided in the green phase.
 */

import type { WorkflowGraph } from './workflow-types.js';

export function parseRalphYaml(_rawYaml: string): WorkflowGraph {
	throw new Error('parseRalphYaml is not implemented');
}
