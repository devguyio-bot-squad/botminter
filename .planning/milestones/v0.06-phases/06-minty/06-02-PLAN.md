---
phase: 06-minty
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/bm/src/commands/minty.rs
  - minty/config.yml
  - minty/skills/hire-guide/SKILL.md
  - minty/skills/profile-browser/SKILL.md
  - minty/skills/team-overview/SKILL.md
  - minty/skills/workspace-doctor/SKILL.md
autonomous: true
requirements: [MNTY-01, MNTY-03]
gap_closure: true

must_haves:
  truths:
    - "bm minty launches the coding agent with CWD set to the Minty config directory (~/.config/botminter/minty/)"
    - "Claude Code discovers Minty skills via .claude/skills/ relative to CWD"
  artifacts:
    - path: "crates/bm/src/commands/minty.rs"
      provides: ".current_dir(&minty_dir) on Command builder before .exec()"
      contains: ".current_dir"
    - path: "minty/config.yml"
      provides: "Updated skills_dir pointing to .claude/skills/"
      contains: ".claude/skills"
    - path: "minty/.claude/skills/hire-guide/SKILL.md"
      provides: "Hire guide skill at Claude Code discovery path"
    - path: "minty/.claude/skills/profile-browser/SKILL.md"
      provides: "Profile browser skill at Claude Code discovery path"
    - path: "minty/.claude/skills/team-overview/SKILL.md"
      provides: "Team overview skill at Claude Code discovery path"
    - path: "minty/.claude/skills/workspace-doctor/SKILL.md"
      provides: "Workspace doctor skill at Claude Code discovery path"
  key_links:
    - from: "crates/bm/src/commands/minty.rs"
      to: "~/.config/botminter/minty/"
      via: ".current_dir(&minty_dir)"
      pattern: "\\.current_dir\\(&minty_dir\\)"
    - from: "minty/.claude/skills/"
      to: "Claude Code skill discovery"
      via: ".claude/skills/ convention"
      pattern: "\\.claude/skills/"
---

<objective>
Fix two UAT gaps from phase 06 validation: (1) `bm minty` not setting CWD to the Minty config directory before launching Claude, and (2) Minty skills placed at `skills/` instead of `.claude/skills/` where Claude Code discovers them.

Purpose: Without these fixes, Claude launches in the caller's CWD (wrong directory) and cannot discover Minty's skills.
Output: Working `bm minty` that launches Claude in the correct directory with discoverable skills.
</objective>

<execution_context>
@/home/sandboxed/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandboxed/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-minty/06-01-SUMMARY.md
@.planning/phases/06-minty/06-UAT.md

<interfaces>
<!-- Reference implementation from chat.rs showing the correct .current_dir() pattern -->

From crates/bm/src/commands/chat.rs (lines 156-161):
```rust
use std::os::unix::process::CommandExt;
let err = std::process::Command::new(&coding_agent.binary)
    .current_dir(&ws_path)
    .arg("--append-system-prompt-file")
    .arg(&tmp_path)
    .exec();
```

From crates/bm/src/commands/minty.rs (lines 39-43, CURRENT — missing .current_dir):
```rust
use std::os::unix::process::CommandExt;
let err = std::process::Command::new(&binary)
    .arg("--append-system-prompt-file")
    .arg(&prompt_path)
    .exec();
```

From crates/bm/src/profile.rs — extract_minty_to_disk uses write_dir_verbatim which copies the embedded tree structure as-is to the target directory.

From minty/config.yml (CURRENT — skills_dir points to old path):
```yaml
skills_dir: skills
```
Must be updated to:
```yaml
skills_dir: .claude/skills
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate embedded skills to .claude/skills/, update config.yml, and add .current_dir()</name>
  <files>
    crates/bm/src/commands/minty.rs,
    minty/config.yml,
    minty/.claude/skills/hire-guide/SKILL.md,
    minty/.claude/skills/profile-browser/SKILL.md,
    minty/.claude/skills/team-overview/SKILL.md,
    minty/.claude/skills/workspace-doctor/SKILL.md
  </files>
  <action>
Three changes:

**Fix 1 — .current_dir() in minty.rs (line 40):**
Add `.current_dir(&minty_dir)` to the Command builder between `.new(&binary)` and `.arg("--append-system-prompt-file")`. This matches the pattern in `chat.rs:158`. The result should be:

```rust
let err = std::process::Command::new(&binary)
    .current_dir(&minty_dir)
    .arg("--append-system-prompt-file")
    .arg(&prompt_path)
    .exec();
```

**Fix 2 — Move skills/ to .claude/skills/ in the embedded minty/ directory:**
Move the four skill directories from `minty/skills/` to `minty/.claude/skills/`:
```
git mv minty/skills/hire-guide minty/.claude/skills/hire-guide
git mv minty/skills/profile-browser minty/.claude/skills/profile-browser
git mv minty/skills/team-overview minty/.claude/skills/team-overview
git mv minty/skills/workspace-doctor minty/.claude/skills/workspace-doctor
```
Then remove the now-empty `minty/skills/` directory.

**Fix 3 — Update minty/config.yml skills_dir:**
Change `skills_dir: skills` to `skills_dir: .claude/skills` in `minty/config.yml`. This ensures any code that reads config.yml for the skills path will resolve correctly against the new location.

Since `write_dir_verbatim()` copies the embedded tree as-is, the extracted Minty config will now have `.claude/skills/` at the correct path relative to CWD, which is what Claude Code expects.

**Update the unit test** `ensure_minty_initialized_creates_config` in minty.rs — after extraction, assert that `.claude/skills/hire-guide/SKILL.md` exists (instead of `skills/hire-guide/SKILL.md` or whatever the current assertion checks).
  </action>
  <verify>
    <automated>cd /home/sandboxed/workspace/botminter && cargo test -p bm -- minty && cargo clippy -p bm -- -D warnings</automated>
  </verify>
  <done>
    - `minty.rs` has `.current_dir(&minty_dir)` on the Command builder
    - `minty/config.yml` has `skills_dir: .claude/skills` (not `skills_dir: skills`)
    - Embedded skills live at `minty/.claude/skills/` (not `minty/skills/`)
    - `minty/skills/` directory no longer exists
    - All tests pass, clippy clean
  </done>
</task>

</tasks>

<verification>
1. `cargo test -p bm -- minty` — all minty-related tests pass
2. `cargo clippy -p bm -- -D warnings` — no warnings
3. `grep -n "current_dir" crates/bm/src/commands/minty.rs` — confirms .current_dir(&minty_dir) present
4. `grep 'skills_dir:' minty/config.yml` — shows `.claude/skills` (not `skills`)
5. `ls minty/.claude/skills/` — shows 4 skill directories
6. `test -d minty/skills && echo "FAIL: old skills/ still exists" || echo "OK: old skills/ removed"`
</verification>

<success_criteria>
- `bm minty` Command builder includes `.current_dir(&minty_dir)` matching chat.rs pattern
- `minty/config.yml` has `skills_dir: .claude/skills`
- Embedded Minty skills located at `minty/.claude/skills/` so they extract to `~/.config/botminter/minty/.claude/skills/`
- Old `minty/skills/` directory removed
- All tests pass, clippy clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-minty/06-02-SUMMARY.md`
</output>
